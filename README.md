# BA Shell Scripts Template
This is the centralized repository of all the shell helper functions we use.

## Usage
In your script, we have call it via `source ${GITLAB_HOME}/shell-scripts-template/common_lib.sh`
